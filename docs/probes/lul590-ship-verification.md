# LUL-590 verification probe

Docs-only throwaway confirming the `[ship]` lane merges end to end after
LUL-558/PR #110 wired `gh pr review --approve` into `automerge.yml` ahead of
`gh pr merge`. Safe to delete once observed merged.
